// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: inventory_query.sql

package sqlc

import (
	"context"
	"strings"
)

const fetchCurrentInventoryForItem = `-- name: FetchCurrentInventoryForItem :one
SELECT
    CAST(COALESCE(
        (SELECT SUM(q.value) FROM inventory_receipt ir
         JOIN quantity q ON ir.quantity_id = q.id
         WHERE ir.item_id = ?
         AND ir.owner_account_id = ?
         AND ir.status_code = 'available'), 0
    ) - COALESCE(
        (SELECT SUM(q.value) FROM inventory_issue ii
         JOIN quantity q ON ii.quantity_id = q.id
         WHERE ii.item_id = ?
         AND ii.account_id = ?
         AND ii.status_code = 'committed'), 0
    ) AS SIGNED) AS available_to_promise,
    COALESCE(
        (SELECT qu.abbreviation FROM item i
         JOIN item_category ic ON i.item_category_id = ic.id
         JOIN unit_group ug ON ic.unit_group_id = ug.id
         JOIN unit qu ON ug.base_unit_id = qu.id
         WHERE i.id = ? LIMIT 1), ''
    ) AS unit_abbreviation
`

type FetchCurrentInventoryForItemParams struct {
	ItemID         string
	OwnerAccountID string
}

type FetchCurrentInventoryForItemRow struct {
	AvailableToPromise int64
	UnitAbbreviation   interface{}
}

func (q *Queries) FetchCurrentInventoryForItem(ctx context.Context, arg FetchCurrentInventoryForItemParams) (FetchCurrentInventoryForItemRow, error) {
	row := q.db.QueryRowContext(ctx, fetchCurrentInventoryForItem,
		arg.ItemID,
		arg.OwnerAccountID,
		arg.ItemID,
		arg.OwnerAccountID,
		arg.ItemID,
	)
	var i FetchCurrentInventoryForItemRow
	err := row.Scan(&i.AvailableToPromise, &i.UnitAbbreviation)
	return i, err
}

const fetchOnHandInventoryBulk = `-- name: FetchOnHandInventoryBulk :many
SELECT
    i.id AS item_id,
    CAST(COALESCE(
        (SELECT SUM(CAST(q.value AS DECIMAL(65,30)))
         FROM inventory_receipt ir
         JOIN quantity q ON ir.quantity_id = q.id
         LEFT JOIN inventory_allocation ia ON ia.inventory_receipt_id = ir.id
         WHERE ir.item_id = i.id
         AND (ir.owner_account_id = ? OR ir.holder_account_id = ?)
         AND ir.status_code = 'available')
    , 0) - COALESCE(
        (SELECT SUM(CAST(aq.value AS DECIMAL(65,30)))
         FROM inventory_receipt ir2
         JOIN inventory_allocation ia2 ON ia2.inventory_receipt_id = ir2.id
         JOIN quantity aq ON aq.id = ia2.quantity_id
         WHERE ir2.item_id = i.id
         AND (ir2.owner_account_id = ? OR ir2.holder_account_id = ?)
         AND ir2.status_code = 'available')
    , 0) AS SIGNED) AS on_hand_quantity,
    bu.id AS unit_id,
    bu.abbreviation AS unit_abbreviation,
    ug.unit_type_code AS unit_type
FROM item i
JOIN item_category ic ON i.item_category_id = ic.id
JOIN unit_group ug ON ic.unit_group_id = ug.id
JOIN unit bu ON ug.base_unit_id = bu.id
WHERE i.id IN (/*SLICE:item_ids*/?)
  AND i.account_id = ?
  AND i.deleted_at IS NULL
`

type FetchOnHandInventoryBulkParams struct {
	OwnerAccountID string
	ItemIds        []string
}

type FetchOnHandInventoryBulkRow struct {
	ItemID           string
	OnHandQuantity   int64
	UnitID           string
	UnitAbbreviation string
	UnitType         string
}

func (q *Queries) FetchOnHandInventoryBulk(ctx context.Context, arg FetchOnHandInventoryBulkParams) ([]FetchOnHandInventoryBulkRow, error) {
	query := fetchOnHandInventoryBulk
	var queryParams []interface{}
	queryParams = append(queryParams, arg.OwnerAccountID)
	queryParams = append(queryParams, arg.OwnerAccountID)
	queryParams = append(queryParams, arg.OwnerAccountID)
	queryParams = append(queryParams, arg.OwnerAccountID)
	if len(arg.ItemIds) > 0 {
		for _, v := range arg.ItemIds {
			queryParams = append(queryParams, v)
		}
		query = strings.Replace(query, "/*SLICE:item_ids*/?", strings.Repeat(",?", len(arg.ItemIds))[1:], 1)
	} else {
		query = strings.Replace(query, "/*SLICE:item_ids*/?", "NULL", 1)
	}
	queryParams = append(queryParams, arg.OwnerAccountID)
	rows, err := q.db.QueryContext(ctx, query, queryParams...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []FetchOnHandInventoryBulkRow
	for rows.Next() {
		var i FetchOnHandInventoryBulkRow
		if err := rows.Scan(
			&i.ItemID,
			&i.OnHandQuantity,
			&i.UnitID,
			&i.UnitAbbreviation,
			&i.UnitType,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
