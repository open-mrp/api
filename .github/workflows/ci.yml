name: CI

on:
  pull_request:
    branches: [main]

jobs:
  changes:
    name: Detect Changes
    runs-on: ubuntu-latest
    outputs:
      go: ${{ steps.filter.outputs.go }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            go:
              - '**/*.go'
              - 'go.mod'
              - 'go.sum'
              - 'tools/tool-versions'
              - 'shared/db/migrations/**'

  lint:
    name: Lint
    needs: [changes]
    if: needs.changes.outputs.go == 'true'
    runs-on: ubuntu-latest
    env:
      CGO_ENABLED: 0
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Validate version
        run: |
          source ./scripts/version.sh
          validate_version "$(go run ./cmd/print-version)" || exit 1

      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@v7
        with:
          version: v2.1.2

  test:
    name: Test
    needs: [changes]
    if: needs.changes.outputs.go == 'true'
    runs-on: ubuntu-latest
    env:
      GOTOOLCHAIN: local
    services:
      mysql:
        image: mysql:8
        env:
          MYSQL_ROOT_PASSWORD: "Testing123!"
          MYSQL_DATABASE: augno
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h localhost -uroot -pTesting123!"
          --health-interval=5s
          --health-timeout=5s
          --health-retries=10
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: augno_agents
          POSTGRES_USER: augno
          POSTGRES_HOST_AUTH_METHOD: trust
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U augno -d augno_agents"
          --health-interval=5s
          --health-timeout=5s
          --health-retries=10
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Cache test tools
        uses: actions/cache@v4
        id: cache-test-tools
        with:
          path: /usr/local/bin/ci-tools
          key: ${{ runner.os }}-test-tools-${{ hashFiles('tools/tool-versions') }}

      - name: Install test tools
        if: steps.cache-test-tools.outputs.cache-hit != 'true'
        run: |
          mkdir -p /usr/local/bin/ci-tools
          SQLC_V=$(awk '/sqlc/{gsub(/^v/,"",$2); print $2}' tools/tool-versions)
          curl -sSL "https://github.com/sqlc-dev/sqlc/releases/download/v${SQLC_V}/sqlc_${SQLC_V}_linux_amd64.tar.gz" | tar xz -C /usr/local/bin/ci-tools sqlc
          GOOSE_V=$(awk '/goose/{gsub(/^v/,"",$2); print $2}' tools/tool-versions)
          curl -sSL "https://github.com/pressly/goose/releases/download/v${GOOSE_V}/goose_linux_x86_64" -o /usr/local/bin/ci-tools/goose
          chmod +x /usr/local/bin/ci-tools/goose

      - name: Add tools to PATH
        run: echo "/usr/local/bin/ci-tools" >> $GITHUB_PATH

      - name: Generate sqlc
        run: make sqlc

      - name: Apply MySQL migrations
        run: |
          GOOSE_DRIVER=mysql \
          GOOSE_DBSTRING="root:Testing123!@tcp(localhost:3306)/augno?parseTime=true" \
            goose -dir shared/db/migrations up

      - name: Apply PostgreSQL migrations
        run: |
          GOOSE_DRIVER=postgres \
          GOOSE_DBSTRING="postgres://augno@localhost:5432/augno_agents?sslmode=disable" \
            goose -dir services/agent-service/db/migrations up

      - name: Run Tests
        run: make test
