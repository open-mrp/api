The API Gateway terminates HTTP requests, authenticates callers, enforces coarse authorization, and routes requests to backend services. It extracts credentials, calls the Auth Service (or validates JWTs locally), constructs an identity context, applies rate limiting and request logging, and forwards identity to downstream services. It shapes internal service responses into public API resources and returns them to clients.