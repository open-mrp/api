// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: notification_preference.sql

package sqlc

import (
	"context"
)

const getEffectiveNotificationPreference = `-- name: GetEffectiveNotificationPreference :one
SELECT id, account_id, account_user_id, category, in_app_enabled, email_enabled, push_enabled, digest, created_at, updated_at FROM notification_preference
WHERE account_user_id = ?
  AND category IN (?, '')
ORDER BY (category = ?) DESC
LIMIT 1
`

type GetEffectiveNotificationPreferenceParams struct {
	AccountUserID string
	Category      string
}

// Resolves the most specific preference for a recipient + category: a category-specific row wins
// over the global (category = ”) default. Returns no row when neither exists (caller defaults).
func (q *Queries) GetEffectiveNotificationPreference(ctx context.Context, arg GetEffectiveNotificationPreferenceParams) (NotificationPreference, error) {
	row := q.db.QueryRowContext(ctx, getEffectiveNotificationPreference, arg.AccountUserID, arg.Category, arg.Category)
	var i NotificationPreference
	err := row.Scan(
		&i.ID,
		&i.AccountID,
		&i.AccountUserID,
		&i.Category,
		&i.InAppEnabled,
		&i.EmailEnabled,
		&i.PushEnabled,
		&i.Digest,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getNotificationPreferenceByUserCategory = `-- name: GetNotificationPreferenceByUserCategory :one
SELECT id, account_id, account_user_id, category, in_app_enabled, email_enabled, push_enabled, digest, created_at, updated_at FROM notification_preference
WHERE account_user_id = ? AND category = ?
`

type GetNotificationPreferenceByUserCategoryParams struct {
	AccountUserID string
	Category      string
}

func (q *Queries) GetNotificationPreferenceByUserCategory(ctx context.Context, arg GetNotificationPreferenceByUserCategoryParams) (NotificationPreference, error) {
	row := q.db.QueryRowContext(ctx, getNotificationPreferenceByUserCategory, arg.AccountUserID, arg.Category)
	var i NotificationPreference
	err := row.Scan(
		&i.ID,
		&i.AccountID,
		&i.AccountUserID,
		&i.Category,
		&i.InAppEnabled,
		&i.EmailEnabled,
		&i.PushEnabled,
		&i.Digest,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listNotificationPreferences = `-- name: ListNotificationPreferences :many
SELECT id, account_id, account_user_id, category, in_app_enabled, email_enabled, push_enabled, digest, created_at, updated_at FROM notification_preference
WHERE account_user_id = ?
ORDER BY category ASC
`

func (q *Queries) ListNotificationPreferences(ctx context.Context, accountUserID string) ([]NotificationPreference, error) {
	rows, err := q.db.QueryContext(ctx, listNotificationPreferences, accountUserID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []NotificationPreference
	for rows.Next() {
		var i NotificationPreference
		if err := rows.Scan(
			&i.ID,
			&i.AccountID,
			&i.AccountUserID,
			&i.Category,
			&i.InAppEnabled,
			&i.EmailEnabled,
			&i.PushEnabled,
			&i.Digest,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const upsertNotificationPreference = `-- name: UpsertNotificationPreference :exec
INSERT INTO notification_preference (
    id, account_id, account_user_id, category,
    in_app_enabled, email_enabled, push_enabled, digest, created_at, updated_at
) VALUES (?, ?, ?, ?, ?, ?, ?, ?, NOW(3), NOW(3))
ON DUPLICATE KEY UPDATE
    in_app_enabled = VALUES(in_app_enabled),
    email_enabled = VALUES(email_enabled),
    push_enabled = VALUES(push_enabled),
    digest = VALUES(digest),
    updated_at = NOW(3)
`

type UpsertNotificationPreferenceParams struct {
	ID            string
	AccountID     string
	AccountUserID string
	Category      string
	InAppEnabled  bool
	EmailEnabled  bool
	PushEnabled   bool
	Digest        string
}

func (q *Queries) UpsertNotificationPreference(ctx context.Context, arg UpsertNotificationPreferenceParams) error {
	_, err := q.db.ExecContext(ctx, upsertNotificationPreference,
		arg.ID,
		arg.AccountID,
		arg.AccountUserID,
		arg.Category,
		arg.InAppEnabled,
		arg.EmailEnabled,
		arg.PushEnabled,
		arg.Digest,
	)
	return err
}
