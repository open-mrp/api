// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: account.sql

package sqlc

import (
	"context"
)

const getAccountTypeCode = `-- name: GetAccountTypeCode :one
SELECT account_type_code FROM account WHERE id = ?
`

// Resolves whether an account is a sandbox. The send path reads this rather than trusting a caller
// to have populated AccountMode on the message: a publisher that forgets the field would otherwise
// mail a sandbox account's invoice to its real customer.
func (q *Queries) GetAccountTypeCode(ctx context.Context, id string) (string, error) {
	row := q.db.QueryRowContext(ctx, getAccountTypeCode, id)
	var account_type_code string
	err := row.Scan(&account_type_code)
	return account_type_code, err
}
