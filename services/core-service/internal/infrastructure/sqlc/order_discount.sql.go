// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: order_discount.sql

package sqlc

import (
	"context"
	"database/sql"
	"time"
)

const checkOrderDiscountDuplicateUsage = `-- name: CheckOrderDiscountDuplicateUsage :one
SELECT COUNT(*) AS count
FROM sales_order
WHERE order_discount_id = ?
AND buyer_account_id = ?
AND seller_account_id = ?
AND owner_account_id = ?
AND (? IS NULL OR id != ?)
`

type CheckOrderDiscountDuplicateUsageParams struct {
	OrderDiscountID sql.NullString
	BuyerAccountID  string
	SellerAccountID string
	OwnerAccountID  string
	ExcludeOrderID  sql.NullString
}

func (q *Queries) CheckOrderDiscountDuplicateUsage(ctx context.Context, arg CheckOrderDiscountDuplicateUsageParams) (int64, error) {
	row := q.db.QueryRowContext(ctx, checkOrderDiscountDuplicateUsage,
		arg.OrderDiscountID,
		arg.BuyerAccountID,
		arg.SellerAccountID,
		arg.OwnerAccountID,
		arg.ExcludeOrderID,
		arg.ExcludeOrderID,
	)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const countOrderDiscountsByCode = `-- name: CountOrderDiscountsByCode :one
SELECT COUNT(*) AS count
FROM order_discount
WHERE account_id = ?
AND code = ?
AND (? IS NULL OR id != ?)
`

type CountOrderDiscountsByCodeParams struct {
	AccountID string
	Code      string
	ExcludeID sql.NullString
}

func (q *Queries) CountOrderDiscountsByCode(ctx context.Context, arg CountOrderDiscountsByCodeParams) (int64, error) {
	row := q.db.QueryRowContext(ctx, countOrderDiscountsByCode,
		arg.AccountID,
		arg.Code,
		arg.ExcludeID,
		arg.ExcludeID,
	)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const deleteOrderDiscount = `-- name: DeleteOrderDiscount :execresult
DELETE FROM order_discount
WHERE id = ?
AND account_id = ?
`

type DeleteOrderDiscountParams struct {
	ID        string
	AccountID string
}

func (q *Queries) DeleteOrderDiscount(ctx context.Context, arg DeleteOrderDiscountParams) (sql.Result, error) {
	return q.db.ExecContext(ctx, deleteOrderDiscount, arg.ID, arg.AccountID)
}

const findOrderDiscountByCode = `-- name: FindOrderDiscountByCode :one
SELECT
    od.id,
    od.name,
    od.code,
    od.percentage,
    od.value,
    od.discount_type_code,
    od.account_id,
    (SELECT COUNT(*) FROM sales_order so WHERE so.order_discount_id = od.id) AS order_count,
    od.created_at,
    od.updated_at
FROM order_discount od
WHERE od.code = ?
AND od.account_id = ?
`

type FindOrderDiscountByCodeParams struct {
	Code      string
	AccountID string
}

type FindOrderDiscountByCodeRow struct {
	ID               string
	Name             string
	Code             string
	Percentage       float64
	Value            float64
	DiscountTypeCode string
	AccountID        string
	OrderCount       int64
	CreatedAt        time.Time
	UpdatedAt        time.Time
}

func (q *Queries) FindOrderDiscountByCode(ctx context.Context, arg FindOrderDiscountByCodeParams) (FindOrderDiscountByCodeRow, error) {
	row := q.db.QueryRowContext(ctx, findOrderDiscountByCode, arg.Code, arg.AccountID)
	var i FindOrderDiscountByCodeRow
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Code,
		&i.Percentage,
		&i.Value,
		&i.DiscountTypeCode,
		&i.AccountID,
		&i.OrderCount,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getOrderDiscount = `-- name: GetOrderDiscount :one
SELECT
    od.id,
    od.name,
    od.code,
    od.percentage,
    od.value,
    od.discount_type_code,
    od.account_id,
    (SELECT COUNT(*) FROM sales_order so WHERE so.order_discount_id = od.id) AS order_count,
    od.created_at,
    od.updated_at
FROM order_discount od
WHERE od.id = ?
AND od.account_id = ?
`

type GetOrderDiscountParams struct {
	ID        string
	AccountID string
}

type GetOrderDiscountRow struct {
	ID               string
	Name             string
	Code             string
	Percentage       float64
	Value            float64
	DiscountTypeCode string
	AccountID        string
	OrderCount       int64
	CreatedAt        time.Time
	UpdatedAt        time.Time
}

func (q *Queries) GetOrderDiscount(ctx context.Context, arg GetOrderDiscountParams) (GetOrderDiscountRow, error) {
	row := q.db.QueryRowContext(ctx, getOrderDiscount, arg.ID, arg.AccountID)
	var i GetOrderDiscountRow
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Code,
		&i.Percentage,
		&i.Value,
		&i.DiscountTypeCode,
		&i.AccountID,
		&i.OrderCount,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const insertOrderDiscount = `-- name: InsertOrderDiscount :exec
INSERT INTO order_discount (id, name, code, percentage, value, discount_type_code, account_id, created_at, updated_at)
VALUES (?, ?, ?, ?, ?, ?, ?, NOW(), NOW())
`

type InsertOrderDiscountParams struct {
	ID               string
	Name             string
	Code             string
	Percentage       float64
	Value            float64
	DiscountTypeCode string
	AccountID        string
}

func (q *Queries) InsertOrderDiscount(ctx context.Context, arg InsertOrderDiscountParams) error {
	_, err := q.db.ExecContext(ctx, insertOrderDiscount,
		arg.ID,
		arg.Name,
		arg.Code,
		arg.Percentage,
		arg.Value,
		arg.DiscountTypeCode,
		arg.AccountID,
	)
	return err
}

const listOrderDiscountsBackward = `-- name: ListOrderDiscountsBackward :many
SELECT
    od.id,
    od.name,
    od.code,
    od.percentage,
    od.value,
    od.discount_type_code,
    od.account_id,
    (SELECT COUNT(*) FROM sales_order so WHERE so.order_discount_id = od.id) AS order_count,
    od.created_at,
    od.updated_at
FROM order_discount od
WHERE od.account_id = ?
AND (
    ? IS NULL
    OR od.name LIKE ?
    OR od.code LIKE ?
)
AND (
    od.created_at > ?
    OR (od.created_at = ? AND od.id > ?)
)
ORDER BY od.created_at ASC, od.id ASC
LIMIT ?
`

type ListOrderDiscountsBackwardParams struct {
	AccountID       string
	SearchQuery     sql.NullString
	CursorCreatedAt time.Time
	CursorID        string
	Limit           int32
}

type ListOrderDiscountsBackwardRow struct {
	ID               string
	Name             string
	Code             string
	Percentage       float64
	Value            float64
	DiscountTypeCode string
	AccountID        string
	OrderCount       int64
	CreatedAt        time.Time
	UpdatedAt        time.Time
}

func (q *Queries) ListOrderDiscountsBackward(ctx context.Context, arg ListOrderDiscountsBackwardParams) ([]ListOrderDiscountsBackwardRow, error) {
	rows, err := q.db.QueryContext(ctx, listOrderDiscountsBackward,
		arg.AccountID,
		arg.SearchQuery,
		arg.SearchQuery,
		arg.SearchQuery,
		arg.CursorCreatedAt,
		arg.CursorCreatedAt,
		arg.CursorID,
		arg.Limit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListOrderDiscountsBackwardRow
	for rows.Next() {
		var i ListOrderDiscountsBackwardRow
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Code,
			&i.Percentage,
			&i.Value,
			&i.DiscountTypeCode,
			&i.AccountID,
			&i.OrderCount,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listOrderDiscountsForward = `-- name: ListOrderDiscountsForward :many
SELECT
    od.id,
    od.name,
    od.code,
    od.percentage,
    od.value,
    od.discount_type_code,
    od.account_id,
    (SELECT COUNT(*) FROM sales_order so WHERE so.order_discount_id = od.id) AS order_count,
    od.created_at,
    od.updated_at
FROM order_discount od
WHERE od.account_id = ?
AND (
    ? IS NULL
    OR od.name LIKE ?
    OR od.code LIKE ?
)
AND (
    ? IS NULL
    OR od.created_at < ?
    OR (od.created_at = ? AND od.id < ?)
)
ORDER BY od.created_at DESC, od.id DESC
LIMIT ?
`

type ListOrderDiscountsForwardParams struct {
	AccountID       string
	SearchQuery     sql.NullString
	CursorCreatedAt sql.NullTime
	CursorID        sql.NullString
	Limit           int32
}

type ListOrderDiscountsForwardRow struct {
	ID               string
	Name             string
	Code             string
	Percentage       float64
	Value            float64
	DiscountTypeCode string
	AccountID        string
	OrderCount       int64
	CreatedAt        time.Time
	UpdatedAt        time.Time
}

func (q *Queries) ListOrderDiscountsForward(ctx context.Context, arg ListOrderDiscountsForwardParams) ([]ListOrderDiscountsForwardRow, error) {
	rows, err := q.db.QueryContext(ctx, listOrderDiscountsForward,
		arg.AccountID,
		arg.SearchQuery,
		arg.SearchQuery,
		arg.SearchQuery,
		arg.CursorCreatedAt,
		arg.CursorCreatedAt,
		arg.CursorCreatedAt,
		arg.CursorID,
		arg.Limit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListOrderDiscountsForwardRow
	for rows.Next() {
		var i ListOrderDiscountsForwardRow
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Code,
			&i.Percentage,
			&i.Value,
			&i.DiscountTypeCode,
			&i.AccountID,
			&i.OrderCount,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateOrderDiscount = `-- name: UpdateOrderDiscount :execresult
UPDATE order_discount
SET
    name = COALESCE(?, name),
    code = COALESCE(?, code),
    percentage = COALESCE(?, percentage),
    value = COALESCE(?, value),
    discount_type_code = COALESCE(?, discount_type_code),
    updated_at = NOW()
WHERE id = ?
AND account_id = ?
`

type UpdateOrderDiscountParams struct {
	Name             sql.NullString
	Code             sql.NullString
	Percentage       sql.NullFloat64
	Value            sql.NullFloat64
	DiscountTypeCode sql.NullString
	ID               string
	AccountID        string
}

func (q *Queries) UpdateOrderDiscount(ctx context.Context, arg UpdateOrderDiscountParams) (sql.Result, error) {
	return q.db.ExecContext(ctx, updateOrderDiscount,
		arg.Name,
		arg.Code,
		arg.Percentage,
		arg.Value,
		arg.DiscountTypeCode,
		arg.ID,
		arg.AccountID,
	)
}
