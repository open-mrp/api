// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: unit.sql

package sqlc

import (
	"context"
	"database/sql"
	"strings"
	"time"
)

const listUnitsBackward = `-- name: ListUnitsBackward :many
SELECT
    unit.id,
    unit.name,
    unit.abbreviation,
    unit.unit_dimension_code,
    unit.ratio_numerator,
    unit.ratio_denominator,
    unit.offset_numerator,
    unit.offset_denominator,
    unit.is_base_unit,
    unit.account_id,
    unit.created_at,
    unit.updated_at
FROM unit
WHERE (unit.account_id = ? OR unit.account_id IS NULL)
AND (? IS NULL OR unit.unit_dimension_code = ?)
AND (
    ? = false
    OR EXISTS (
        SELECT 1 FROM unit_group_unit
        JOIN unit_group ON unit_group_unit.unit_group_id = unit_group.id
        WHERE unit_group_unit.unit_id = unit.id
        AND unit_group_unit.unit_group_id IN (/*SLICE:unit_group_ids*/?)
        AND (unit_group.account_id = ? OR unit_group.account_id IS NULL)
    )
)
AND (
    ? IS NULL
    OR unit.name LIKE ?
    OR unit.abbreviation = ?
)
AND (
    unit.created_at > ?
    OR (unit.created_at = ? AND unit.id > ?)
)
ORDER BY unit.created_at ASC, unit.id ASC
LIMIT ?
`

type ListUnitsBackwardParams struct {
	AccountID              sql.NullString
	UnitDimensionCode      sql.NullString
	IncludeUnitGroupFilter interface{}
	UnitGroupIds           []string
	SearchQuery            sql.NullString
	SearchExact            sql.NullString
	CursorCreatedAt        time.Time
	CursorID               string
	Limit                  int32
}

type ListUnitsBackwardRow struct {
	ID                string
	Name              string
	Abbreviation      string
	UnitDimensionCode string
	RatioNumerator    string
	RatioDenominator  string
	OffsetNumerator   string
	OffsetDenominator string
	IsBaseUnit        bool
	AccountID         sql.NullString
	CreatedAt         time.Time
	UpdatedAt         time.Time
}

func (q *Queries) ListUnitsBackward(ctx context.Context, arg ListUnitsBackwardParams) ([]ListUnitsBackwardRow, error) {
	query := listUnitsBackward
	var queryParams []interface{}
	queryParams = append(queryParams, arg.AccountID)
	queryParams = append(queryParams, arg.UnitDimensionCode)
	queryParams = append(queryParams, arg.UnitDimensionCode)
	queryParams = append(queryParams, arg.IncludeUnitGroupFilter)
	if len(arg.UnitGroupIds) > 0 {
		for _, v := range arg.UnitGroupIds {
			queryParams = append(queryParams, v)
		}
		query = strings.Replace(query, "/*SLICE:unit_group_ids*/?", strings.Repeat(",?", len(arg.UnitGroupIds))[1:], 1)
	} else {
		query = strings.Replace(query, "/*SLICE:unit_group_ids*/?", "NULL", 1)
	}
	queryParams = append(queryParams, arg.AccountID)
	queryParams = append(queryParams, arg.SearchQuery)
	queryParams = append(queryParams, arg.SearchQuery)
	queryParams = append(queryParams, arg.SearchExact)
	queryParams = append(queryParams, arg.CursorCreatedAt)
	queryParams = append(queryParams, arg.CursorCreatedAt)
	queryParams = append(queryParams, arg.CursorID)
	queryParams = append(queryParams, arg.Limit)
	rows, err := q.query(ctx, nil, query, queryParams...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListUnitsBackwardRow
	for rows.Next() {
		var i ListUnitsBackwardRow
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Abbreviation,
			&i.UnitDimensionCode,
			&i.RatioNumerator,
			&i.RatioDenominator,
			&i.OffsetNumerator,
			&i.OffsetDenominator,
			&i.IsBaseUnit,
			&i.AccountID,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listUnitsForward = `-- name: ListUnitsForward :many
SELECT
    unit.id,
    unit.name,
    unit.abbreviation,
    unit.unit_dimension_code,
    unit.ratio_numerator,
    unit.ratio_denominator,
    unit.offset_numerator,
    unit.offset_denominator,
    unit.is_base_unit,
    unit.account_id,
    unit.created_at,
    unit.updated_at
FROM unit
WHERE (unit.account_id = ? OR unit.account_id IS NULL)
AND (? IS NULL OR unit.unit_dimension_code = ?)
AND (
    ? = false
    OR EXISTS (
        SELECT 1 FROM unit_group_unit
        JOIN unit_group ON unit_group_unit.unit_group_id = unit_group.id
        WHERE unit_group_unit.unit_id = unit.id
        AND unit_group_unit.unit_group_id IN (/*SLICE:unit_group_ids*/?)
        AND (unit_group.account_id = ? OR unit_group.account_id IS NULL)
    )
)
AND (
    ? IS NULL
    OR unit.name LIKE ?
    OR unit.abbreviation = ?
)
AND (
    ? IS NULL
    OR unit.created_at < ?
    OR (unit.created_at = ? AND unit.id < ?)
)
ORDER BY unit.created_at DESC, unit.id DESC
LIMIT ?
`

type ListUnitsForwardParams struct {
	AccountID              sql.NullString
	UnitDimensionCode      sql.NullString
	IncludeUnitGroupFilter interface{}
	UnitGroupIds           []string
	SearchQuery            sql.NullString
	SearchExact            sql.NullString
	CursorCreatedAt        sql.NullTime
	CursorID               sql.NullString
	Limit                  int32
}

type ListUnitsForwardRow struct {
	ID                string
	Name              string
	Abbreviation      string
	UnitDimensionCode string
	RatioNumerator    string
	RatioDenominator  string
	OffsetNumerator   string
	OffsetDenominator string
	IsBaseUnit        bool
	AccountID         sql.NullString
	CreatedAt         time.Time
	UpdatedAt         time.Time
}

func (q *Queries) ListUnitsForward(ctx context.Context, arg ListUnitsForwardParams) ([]ListUnitsForwardRow, error) {
	query := listUnitsForward
	var queryParams []interface{}
	queryParams = append(queryParams, arg.AccountID)
	queryParams = append(queryParams, arg.UnitDimensionCode)
	queryParams = append(queryParams, arg.UnitDimensionCode)
	queryParams = append(queryParams, arg.IncludeUnitGroupFilter)
	if len(arg.UnitGroupIds) > 0 {
		for _, v := range arg.UnitGroupIds {
			queryParams = append(queryParams, v)
		}
		query = strings.Replace(query, "/*SLICE:unit_group_ids*/?", strings.Repeat(",?", len(arg.UnitGroupIds))[1:], 1)
	} else {
		query = strings.Replace(query, "/*SLICE:unit_group_ids*/?", "NULL", 1)
	}
	queryParams = append(queryParams, arg.AccountID)
	queryParams = append(queryParams, arg.SearchQuery)
	queryParams = append(queryParams, arg.SearchQuery)
	queryParams = append(queryParams, arg.SearchExact)
	queryParams = append(queryParams, arg.CursorCreatedAt)
	queryParams = append(queryParams, arg.CursorCreatedAt)
	queryParams = append(queryParams, arg.CursorCreatedAt)
	queryParams = append(queryParams, arg.CursorID)
	queryParams = append(queryParams, arg.Limit)
	rows, err := q.query(ctx, nil, query, queryParams...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListUnitsForwardRow
	for rows.Next() {
		var i ListUnitsForwardRow
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Abbreviation,
			&i.UnitDimensionCode,
			&i.RatioNumerator,
			&i.RatioDenominator,
			&i.OffsetNumerator,
			&i.OffsetDenominator,
			&i.IsBaseUnit,
			&i.AccountID,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
