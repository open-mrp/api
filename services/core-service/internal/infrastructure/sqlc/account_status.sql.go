// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: account_status.sql

package sqlc

import (
	"context"
	"database/sql"
	"strings"
	"time"
)

const getAccountStatus = `-- name: GetAccountStatus :one
SELECT
    account_status.id,
    account_status.code,
    account_status.name,
    account_status.created_at,
    account_status.updated_at
FROM account_status
WHERE account_status.id = ? OR account_status.code = ?
`

type GetAccountStatusParams struct {
	ID   string
	Code string
}

func (q *Queries) GetAccountStatus(ctx context.Context, arg GetAccountStatusParams) (AccountStatus, error) {
	row := q.db.QueryRowContext(ctx, getAccountStatus, arg.ID, arg.Code)
	var i AccountStatus
	err := row.Scan(
		&i.ID,
		&i.Code,
		&i.Name,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getAccountStatusesByIDs = `-- name: GetAccountStatusesByIDs :many
SELECT
    account_status.id,
    account_status.code,
    account_status.name,
    account_status.created_at,
    account_status.updated_at
FROM account_status
WHERE account_status.id IN (/*SLICE:ids*/?)
`

// System-wide resource; no per-caller scoping.
func (q *Queries) GetAccountStatusesByIDs(ctx context.Context, ids []string) ([]AccountStatus, error) {
	query := getAccountStatusesByIDs
	var queryParams []interface{}
	if len(ids) > 0 {
		for _, v := range ids {
			queryParams = append(queryParams, v)
		}
		query = strings.Replace(query, "/*SLICE:ids*/?", strings.Repeat(",?", len(ids))[1:], 1)
	} else {
		query = strings.Replace(query, "/*SLICE:ids*/?", "NULL", 1)
	}
	rows, err := q.db.QueryContext(ctx, query, queryParams...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []AccountStatus
	for rows.Next() {
		var i AccountStatus
		if err := rows.Scan(
			&i.ID,
			&i.Code,
			&i.Name,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listAccountStatusesBackward = `-- name: ListAccountStatusesBackward :many
SELECT
    account_status.id,
    account_status.code,
    account_status.name,
    account_status.created_at,
    account_status.updated_at
FROM account_status
WHERE (
    ? IS NULL
    OR account_status.name LIKE ?
)
AND (
    account_status.created_at > ?
    OR (account_status.created_at = ? AND account_status.id > ?)
)
ORDER BY account_status.created_at ASC, account_status.id ASC
LIMIT ?
`

type ListAccountStatusesBackwardParams struct {
	SearchQuery     sql.NullString
	CursorCreatedAt time.Time
	CursorID        string
	Limit           int32
}

func (q *Queries) ListAccountStatusesBackward(ctx context.Context, arg ListAccountStatusesBackwardParams) ([]AccountStatus, error) {
	rows, err := q.db.QueryContext(ctx, listAccountStatusesBackward,
		arg.SearchQuery,
		arg.SearchQuery,
		arg.CursorCreatedAt,
		arg.CursorCreatedAt,
		arg.CursorID,
		arg.Limit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []AccountStatus
	for rows.Next() {
		var i AccountStatus
		if err := rows.Scan(
			&i.ID,
			&i.Code,
			&i.Name,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listAccountStatusesForward = `-- name: ListAccountStatusesForward :many
SELECT
    account_status.id,
    account_status.code,
    account_status.name,
    account_status.created_at,
    account_status.updated_at
FROM account_status
WHERE (
    ? IS NULL
    OR account_status.name LIKE ?
)
AND (
    ? IS NULL
    OR account_status.created_at < ?
    OR (account_status.created_at = ? AND account_status.id < ?)
)
ORDER BY account_status.created_at DESC, account_status.id DESC
LIMIT ?
`

type ListAccountStatusesForwardParams struct {
	SearchQuery     sql.NullString
	CursorCreatedAt sql.NullTime
	CursorID        sql.NullString
	Limit           int32
}

func (q *Queries) ListAccountStatusesForward(ctx context.Context, arg ListAccountStatusesForwardParams) ([]AccountStatus, error) {
	rows, err := q.db.QueryContext(ctx, listAccountStatusesForward,
		arg.SearchQuery,
		arg.SearchQuery,
		arg.CursorCreatedAt,
		arg.CursorCreatedAt,
		arg.CursorCreatedAt,
		arg.CursorID,
		arg.Limit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []AccountStatus
	for rows.Next() {
		var i AccountStatus
		if err := rows.Scan(
			&i.ID,
			&i.Code,
			&i.Name,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
