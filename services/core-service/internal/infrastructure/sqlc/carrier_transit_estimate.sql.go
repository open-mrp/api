// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: carrier_transit_estimate.sql

package sqlc

import (
	"context"
	"database/sql"
)

const resolveCarrierTransit = `-- name: ResolveCarrierTransit :one
SELECT
    carrier_option.default_transit_days,
    carrier_transit_estimate.transit_days AS lane_transit_days,
    carrier_transit_estimate.source_code AS lane_source_code,
    carrier_transit_estimate.refreshed_at AS lane_refreshed_at
FROM carrier_option
LEFT JOIN carrier_transit_estimate
    ON carrier_transit_estimate.carrier_option_id = carrier_option.id
    AND carrier_transit_estimate.account_id = ?
    AND carrier_transit_estimate.origin_country = ?
    AND carrier_transit_estimate.origin_postal = ?
    AND carrier_transit_estimate.dest_country = ?
    AND carrier_transit_estimate.dest_postal = ?
WHERE carrier_option.id = ?
AND (carrier_option.account_id = ? OR carrier_option.account_id IS NULL)
`

type ResolveCarrierTransitParams struct {
	AccountID       string
	OriginCountry   string
	OriginPostal    string
	DestCountry     string
	DestPostal      string
	CarrierOptionID string
	OptionAccountID sql.NullString
}

type ResolveCarrierTransitRow struct {
	DefaultTransitDays sql.NullInt32
	LaneTransitDays    sql.NullInt32
	LaneSourceCode     sql.NullString
	LaneRefreshedAt    sql.NullTime
}

// Resolves both transit sources for one lane in a single round trip, so stamping a commitment adds one seek rather than two. The service level always exists (it is the join root); the lane estimate is the optional half, and a NULL lane_transit_days means it has never been warmed.
// Staleness is judged in Go rather than filtered here: the cutoff is a policy constant, and a query that hid stale rows would make "expired" indistinguishable from "never warmed" when deciding whether to fall back.
// Same value as account_id above, under its own name because carrier_option.account_id is nullable (system-owned service levels) and sqlc will not infer one param as both nullable and not.
func (q *Queries) ResolveCarrierTransit(ctx context.Context, arg ResolveCarrierTransitParams) (ResolveCarrierTransitRow, error) {
	row := q.db.QueryRowContext(ctx, resolveCarrierTransit,
		arg.AccountID,
		arg.OriginCountry,
		arg.OriginPostal,
		arg.DestCountry,
		arg.DestPostal,
		arg.CarrierOptionID,
		arg.OptionAccountID,
	)
	var i ResolveCarrierTransitRow
	err := row.Scan(
		&i.DefaultTransitDays,
		&i.LaneTransitDays,
		&i.LaneSourceCode,
		&i.LaneRefreshedAt,
	)
	return i, err
}

const upsertCarrierTransitEstimate = `-- name: UpsertCarrierTransitEstimate :exec
INSERT INTO carrier_transit_estimate (
    id,
    account_id,
    carrier_option_id,
    origin_country,
    origin_postal,
    dest_country,
    dest_postal,
    transit_days,
    source_code,
    refreshed_at,
    created_at,
    updated_at
) VALUES (
    ?,
    ?,
    ?,
    ?,
    ?,
    ?,
    ?,
    ?,
    ?,
    NOW(3),
    NOW(3),
    NOW(3)
)
ON DUPLICATE KEY UPDATE
    transit_days = IF(carrier_transit_estimate.source_code = 'manual', carrier_transit_estimate.transit_days, VALUES(transit_days)),
    refreshed_at = IF(carrier_transit_estimate.source_code = 'manual', carrier_transit_estimate.refreshed_at, NOW(3)),
    updated_at = NOW(3)
`

type UpsertCarrierTransitEstimateParams struct {
	ID              string
	AccountID       string
	CarrierOptionID string
	OriginCountry   string
	OriginPostal    string
	DestCountry     string
	DestPostal      string
	TransitDays     int32
	SourceCode      string
}

// Writes a harvested lane estimate, leaving operator-entered rows alone. The guard is in the statement rather than a read-then-write so two warms racing on the same lane cannot interleave and clobber a manual answer between them.
func (q *Queries) UpsertCarrierTransitEstimate(ctx context.Context, arg UpsertCarrierTransitEstimateParams) error {
	_, err := q.db.ExecContext(ctx, upsertCarrierTransitEstimate,
		arg.ID,
		arg.AccountID,
		arg.CarrierOptionID,
		arg.OriginCountry,
		arg.OriginPostal,
		arg.DestCountry,
		arg.DestPostal,
		arg.TransitDays,
		arg.SourceCode,
	)
	return err
}
