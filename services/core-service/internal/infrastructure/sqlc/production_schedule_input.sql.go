// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: production_schedule_input.sql

package sqlc

import (
	"context"
	"database/sql"
	"strings"
	"time"
)

const getAccountProductionScheduleSetting = `-- name: GetAccountProductionScheduleSetting :one
SELECT
    s.id,
    s.constraint_department_id,
    s.planning_horizon_weeks,
    s.frozen_weeks,
    s.demand_window_months,
    s.forecast_history_months,
    s.forecast_months,
    s.demand_basis_code,
    s.forecast_z,
    s.changeover_avg_minutes,
    s.changeover_min_minutes,
    s.changeover_max_minutes,
    s.changeover_labor_rate,
    s.holding_rate_pct,
    s.service_level_z,
    s.finish_lead_time_weeks,
    s.default_constraint_lead_time_weeks,
    s.max_weeks_supply,
    s.max_flow_depth,
    s.shifts_per_day,
    s.hours_per_shift,
    s.work_days_per_week,
    s.weeks_per_year,
    s.capacity_headroom_pct,
    s.default_lot_units,
    s.default_customer_lead_time_days,
    s.default_fulfillment_policy_code,
    s.recommendation_dormant_months,
    s.recommendation_concentration_pct,
    s.recommendation_adi_threshold,
    s.recommendation_cv2_threshold,
    s.recommendation_slow_mover_cogs,
    s.recommendation_high_value_unit_cost,
    -- The cadence and display columns are read by the settings API, which shares this query so there is one definition of "the merchant's assumptions".
    s.week_start_day,
    s.is_enabled,
    s.generation_cron,
    s.generation_timezone,
    s.auto_publish,
    s.last_generated_at,
    s.created_at,
    s.updated_at
FROM account_production_schedule_setting s
WHERE s.account_id = ?
`

type GetAccountProductionScheduleSettingRow struct {
	ID                              string
	ConstraintDepartmentID          sql.NullString
	PlanningHorizonWeeks            int32
	FrozenWeeks                     int32
	DemandWindowMonths              int32
	ForecastHistoryMonths           int32
	ForecastMonths                  int32
	DemandBasisCode                 string
	ForecastZ                       string
	ChangeoverAvgMinutes            string
	ChangeoverMinMinutes            string
	ChangeoverMaxMinutes            string
	ChangeoverLaborRate             string
	HoldingRatePct                  string
	ServiceLevelZ                   string
	FinishLeadTimeWeeks             string
	DefaultConstraintLeadTimeWeeks  string
	MaxWeeksSupply                  string
	MaxFlowDepth                    int32
	ShiftsPerDay                    int32
	HoursPerShift                   string
	WorkDaysPerWeek                 int32
	WeeksPerYear                    int32
	CapacityHeadroomPct             string
	DefaultLotUnits                 string
	DefaultCustomerLeadTimeDays     int32
	DefaultFulfillmentPolicyCode    string
	RecommendationDormantMonths     int32
	RecommendationConcentrationPct  string
	RecommendationAdiThreshold      string
	RecommendationCv2Threshold      string
	RecommendationSlowMoverCogs     string
	RecommendationHighValueUnitCost string
	WeekStartDay                    int32
	IsEnabled                       bool
	GenerationCron                  sql.NullString
	GenerationTimezone              string
	AutoPublish                     bool
	LastGeneratedAt                 sql.NullTime
	CreatedAt                       time.Time
	UpdatedAt                       time.Time
}

// GetAccountProductionScheduleSetting returns the merchant's planning assumptions. Absent means the account has never configured scheduling, and the caller falls back to code defaults rather than refusing to plan.
func (q *Queries) GetAccountProductionScheduleSetting(ctx context.Context, accountID string) (GetAccountProductionScheduleSettingRow, error) {
	row := q.db.QueryRowContext(ctx, getAccountProductionScheduleSetting, accountID)
	var i GetAccountProductionScheduleSettingRow
	err := row.Scan(
		&i.ID,
		&i.ConstraintDepartmentID,
		&i.PlanningHorizonWeeks,
		&i.FrozenWeeks,
		&i.DemandWindowMonths,
		&i.ForecastHistoryMonths,
		&i.ForecastMonths,
		&i.DemandBasisCode,
		&i.ForecastZ,
		&i.ChangeoverAvgMinutes,
		&i.ChangeoverMinMinutes,
		&i.ChangeoverMaxMinutes,
		&i.ChangeoverLaborRate,
		&i.HoldingRatePct,
		&i.ServiceLevelZ,
		&i.FinishLeadTimeWeeks,
		&i.DefaultConstraintLeadTimeWeeks,
		&i.MaxWeeksSupply,
		&i.MaxFlowDepth,
		&i.ShiftsPerDay,
		&i.HoursPerShift,
		&i.WorkDaysPerWeek,
		&i.WeeksPerYear,
		&i.CapacityHeadroomPct,
		&i.DefaultLotUnits,
		&i.DefaultCustomerLeadTimeDays,
		&i.DefaultFulfillmentPolicyCode,
		&i.RecommendationDormantMonths,
		&i.RecommendationConcentrationPct,
		&i.RecommendationAdiThreshold,
		&i.RecommendationCv2Threshold,
		&i.RecommendationSlowMoverCogs,
		&i.RecommendationHighValueUnitCost,
		&i.WeekStartDay,
		&i.IsEnabled,
		&i.GenerationCron,
		&i.GenerationTimezone,
		&i.AutoPublish,
		&i.LastGeneratedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getActiveDemandOverrides = `-- name: GetActiveDemandOverrides :many
SELECT
    do_.id,
    do_.scope_code,
    do_.scope_ref_id,
    do_.period_start_date,
    do_.period_end_date,
    do_.override_type_code,
    do_.value,
    do_.reason_code,
    do_.created_at
FROM demand_override do_
WHERE do_.account_id = ?
  AND do_.is_active = 1
  AND do_.effective_from <= ?
  AND (do_.expires_at IS NULL OR do_.expires_at > ?)
ORDER BY do_.scope_code, do_.scope_ref_id, do_.period_start_date, do_.created_at
`

type GetActiveDemandOverridesParams struct {
	AccountID  string
	AsOf       time.Time
	AsOfExpiry sql.NullTime
}

type GetActiveDemandOverridesRow struct {
	ID               string
	ScopeCode        string
	ScopeRefID       string
	PeriodStartDate  time.Time
	PeriodEndDate    time.Time
	OverrideTypeCode string
	Value            string
	ReasonCode       sql.NullString
	CreatedAt        time.Time
}

// GetActiveDemandOverrides returns the overrides in force for the planning date. These are the only mechanism for adjusting demand away from history; there is no growth multiplier.
func (q *Queries) GetActiveDemandOverrides(ctx context.Context, arg GetActiveDemandOverridesParams) ([]GetActiveDemandOverridesRow, error) {
	rows, err := q.db.QueryContext(ctx, getActiveDemandOverrides, arg.AccountID, arg.AsOf, arg.AsOfExpiry)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []GetActiveDemandOverridesRow
	for rows.Next() {
		var i GetActiveDemandOverridesRow
		if err := rows.Scan(
			&i.ID,
			&i.ScopeCode,
			&i.ScopeRefID,
			&i.PeriodStartDate,
			&i.PeriodEndDate,
			&i.OverrideTypeCode,
			&i.Value,
			&i.ReasonCode,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getAllSellableProducts = `-- name: GetAllSellableProducts :many
SELECT
    p.id AS product_id,
    p.item_id,
    i.sku,
    p.product_line_id
FROM product p
JOIN item i ON i.id = p.item_id
WHERE i.account_id = ?
ORDER BY i.sku, p.item_id
`

type GetAllSellableProductsRow struct {
	ProductID     string
	ItemID        string
	Sku           string
	ProductLineID sql.NullString
}

// GetAllSellableProducts returns every product in the account with the item and line it carries. The candidate set for a fulfillment recommendation is everything that can be sold, since that is where policy is resolved.
func (q *Queries) GetAllSellableProducts(ctx context.Context, accountID string) ([]GetAllSellableProductsRow, error) {
	rows, err := q.db.QueryContext(ctx, getAllSellableProducts, accountID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []GetAllSellableProductsRow
	for rows.Next() {
		var i GetAllSellableProductsRow
		if err := rows.Scan(
			&i.ProductID,
			&i.ItemID,
			&i.Sku,
			&i.ProductLineID,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getBatchFlowChildren = `-- name: GetBatchFlowChildren :many
SELECT
    bf.B AS parent_batch_id,
    child.id AS batch_id,
    child.item_id
FROM _batch_flow bf
JOIN batch child ON child.id = bf.A
WHERE child.account_id = ?
  AND bf.B IN (/*SLICE:parent_batch_ids*/?)
ORDER BY bf.B, child.id
`

type GetBatchFlowChildrenParams struct {
	AccountID      string
	ParentBatchIds []string
}

type GetBatchFlowChildrenRow struct {
	ParentBatchID string
	BatchID       string
	ItemID        string
}

// GetBatchFlowChildren returns the immediate downstream batches for a set of batches.
//
// The caller walks the genealogy one level at a time, passing the whole frontier each round. That is O(depth) queries total regardless of how many items are being planned, where the TS script issued one query per depth PER item. A recursive CTE would be nicer still, but sqlc's MySQL parser cannot resolve the self-reference. Per the Prisma orientation of _batch_flow (row (A, B): A = downstream/target, B = upstream/source; see docs/patterns/production-step-graph-patterns.md and InsertBatchFlow in batch.sql), a batch's children are the A side of rows where it is B.
func (q *Queries) GetBatchFlowChildren(ctx context.Context, arg GetBatchFlowChildrenParams) ([]GetBatchFlowChildrenRow, error) {
	query := getBatchFlowChildren
	var queryParams []interface{}
	queryParams = append(queryParams, arg.AccountID)
	if len(arg.ParentBatchIds) > 0 {
		for _, v := range arg.ParentBatchIds {
			queryParams = append(queryParams, v)
		}
		query = strings.Replace(query, "/*SLICE:parent_batch_ids*/?", strings.Repeat(",?", len(arg.ParentBatchIds))[1:], 1)
	} else {
		query = strings.Replace(query, "/*SLICE:parent_batch_ids*/?", "NULL", 1)
	}
	rows, err := q.db.QueryContext(ctx, query, queryParams...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []GetBatchFlowChildrenRow
	for rows.Next() {
		var i GetBatchFlowChildrenRow
		if err := rows.Scan(&i.ParentBatchID, &i.BatchID, &i.ItemID); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getConstraintBatchMeasurements = `-- name: GetConstraintBatchMeasurements :many
SELECT
    b.id AS batch_id,
    b.item_id,
    i.sku,
    b.scanned_at,
    q.value AS quantity_value,
    u.abbreviation AS quantity_unit,
    q.unit_id AS quantity_unit_id,
    u.ratio_numerator,
    u.ratio_denominator,
    b.production_step_id,
    bm.B AS machine_id,
    mc.name AS machine_name,
    cost_rate.value AS unit_cost,
    labor_time.value AS labor_time_value,
    labor_time_unit.abbreviation AS labor_time_unit,
    labor_rate.value AS labor_rate,
    overhead_rate.value AS overhead_rate,
    pr.created_at AS run_created_at
FROM batch b
JOIN _batches_machines bm ON bm.A = b.id
JOIN machine mc ON mc.id = bm.B
JOIN item i ON i.id = b.item_id
LEFT JOIN quantity q ON q.id = b.quantity_id
LEFT JOIN unit u ON u.id = q.unit_id
LEFT JOIN rate cost_rate ON cost_rate.id = i.unit_cost_id
LEFT JOIN production_step ps ON ps.id = b.production_step_id
LEFT JOIN scanning_station ss ON ss.id = ps.scanning_station_id
LEFT JOIN rate labor_time ON labor_time.id = ps.labor_time_id
LEFT JOIN unit labor_time_unit ON labor_time_unit.id = labor_time.numerator_unit_id
LEFT JOIN rate labor_rate ON labor_rate.id = ps.labor_rate_id
LEFT JOIN rate overhead_rate ON overhead_rate.id = ps.overhead_rate_id
LEFT JOIN production_run pr ON pr.id = b.production_run_id
WHERE b.account_id = ?
  AND b.scanned_at IS NOT NULL
  AND b.scanned_at >= ?
  AND b.scanned_at <= ?
  AND bm.B IN (/*SLICE:machine_ids*/?)
  -- A constraint machine can carry scans from other stages (a sewing step recorded against a knitting machine). Only steps that belong to the constraint department are measurements of the constraint; a step names its department directly or through the scanning station it is scanned at.
  AND COALESCE(ps.department_id, ss.department_id) = ?
ORDER BY bm.B, b.scanned_at, b.id
`

type GetConstraintBatchMeasurementsParams struct {
	AccountID              string
	WindowStart            sql.NullTime
	WindowEnd              sql.NullTime
	MachineIds             []string
	ConstraintDepartmentID sql.NullString
}

type GetConstraintBatchMeasurementsRow struct {
	BatchID          string
	ItemID           string
	Sku              string
	ScannedAt        sql.NullTime
	QuantityValue    sql.NullString
	QuantityUnit     sql.NullString
	QuantityUnitID   sql.NullString
	RatioNumerator   sql.NullString
	RatioDenominator sql.NullString
	ProductionStepID sql.NullString
	MachineID        string
	MachineName      string
	UnitCost         sql.NullString
	LaborTimeValue   sql.NullString
	LaborTimeUnit    sql.NullString
	LaborRate        sql.NullString
	OverheadRate     sql.NullString
	RunCreatedAt     sql.NullTime
}

// GetConstraintBatchMeasurements returns one row per historical batch produced on the constraint machines, which is what the run rate, cost, lot count, machine affinity and measured lead time are all derived from.
//
// labor_time is a Rate whose numerator unit decides its scale (min/hr/sec); the caller converts. production_run.created_at paired with scanned_at gives the observed lead time for that batch.
func (q *Queries) GetConstraintBatchMeasurements(ctx context.Context, arg GetConstraintBatchMeasurementsParams) ([]GetConstraintBatchMeasurementsRow, error) {
	query := getConstraintBatchMeasurements
	var queryParams []interface{}
	queryParams = append(queryParams, arg.AccountID)
	queryParams = append(queryParams, arg.WindowStart)
	queryParams = append(queryParams, arg.WindowEnd)
	if len(arg.MachineIds) > 0 {
		for _, v := range arg.MachineIds {
			queryParams = append(queryParams, v)
		}
		query = strings.Replace(query, "/*SLICE:machine_ids*/?", strings.Repeat(",?", len(arg.MachineIds))[1:], 1)
	} else {
		query = strings.Replace(query, "/*SLICE:machine_ids*/?", "NULL", 1)
	}
	queryParams = append(queryParams, arg.ConstraintDepartmentID)
	rows, err := q.db.QueryContext(ctx, query, queryParams...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []GetConstraintBatchMeasurementsRow
	for rows.Next() {
		var i GetConstraintBatchMeasurementsRow
		if err := rows.Scan(
			&i.BatchID,
			&i.ItemID,
			&i.Sku,
			&i.ScannedAt,
			&i.QuantityValue,
			&i.QuantityUnit,
			&i.QuantityUnitID,
			&i.RatioNumerator,
			&i.RatioDenominator,
			&i.ProductionStepID,
			&i.MachineID,
			&i.MachineName,
			&i.UnitCost,
			&i.LaborTimeValue,
			&i.LaborTimeUnit,
			&i.LaborRate,
			&i.OverheadRate,
			&i.RunCreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getConstraintDepartmentLaborRate = `-- name: GetConstraintDepartmentLaborRate :one
SELECT r.value
FROM department d
JOIN rate r ON r.id = d.labor_rate_id
WHERE d.account_id = ?
  AND d.id = ?
`

type GetConstraintDepartmentLaborRateParams struct {
	AccountID    string
	DepartmentID string
}

// GetConstraintDepartmentLaborRate returns the hourly labor rate configured on the constraint department, when it has one. The department's own rate prices its changeovers; the account-wide setting is only the fallback.
func (q *Queries) GetConstraintDepartmentLaborRate(ctx context.Context, arg GetConstraintDepartmentLaborRateParams) (string, error) {
	row := q.db.QueryRowContext(ctx, getConstraintDepartmentLaborRate, arg.AccountID, arg.DepartmentID)
	var value string
	err := row.Scan(&value)
	return value, err
}

const getConstraintDepartmentStepCoverage = `-- name: GetConstraintDepartmentStepCoverage :one
SELECT
    COUNT(*) AS machine_count,
    SUM(CASE WHEN m.production_step_id IS NULL THEN 1 ELSE 0 END) AS machines_without_step
FROM machine m
WHERE m.account_id = ?
  AND m.department_id = ?
`

type GetConstraintDepartmentStepCoverageParams struct {
	AccountID    string
	DepartmentID string
}

type GetConstraintDepartmentStepCoverageRow struct {
	MachineCount        int64
	MachinesWithoutStep interface{}
}

// GetConstraintDepartmentStepCoverage reports how much of the constraint department can actually carry a plan downstream.
//
// A campaign explodes into department work through the machine's own production step, so a machine with no step assigned produces campaigns that derive nothing. That is a data problem worth naming rather than guessing at: picking a step for the machine because it happens to share a department would put work in front of a room that was never asked to do it.
func (q *Queries) GetConstraintDepartmentStepCoverage(ctx context.Context, arg GetConstraintDepartmentStepCoverageParams) (GetConstraintDepartmentStepCoverageRow, error) {
	row := q.db.QueryRowContext(ctx, getConstraintDepartmentStepCoverage, arg.AccountID, arg.DepartmentID)
	var i GetConstraintDepartmentStepCoverageRow
	err := row.Scan(&i.MachineCount, &i.MachinesWithoutStep)
	return i, err
}

const getConstraintMachines = `-- name: GetConstraintMachines :many

SELECT
    m.id,
    m.name,
    m.department_id,
    m.production_step_id
FROM machine m
LEFT JOIN production_schedule_resource_setting rs
    ON rs.scope_ref_id = m.id
   AND rs.scope_code = 'machine'
   AND rs.account_id = ?
WHERE m.account_id = ?
  AND m.department_id = ?
  AND COALESCE(rs.is_excluded, 0) = 0
  AND COALESCE(rs.is_enabled, 1) = 1
ORDER BY m.name, m.id
`

type GetConstraintMachinesParams struct {
	AccountID    string
	DepartmentID string
}

type GetConstraintMachinesRow struct {
	ID               string
	Name             string
	DepartmentID     string
	ProductionStepID sql.NullString
}

// Solver inputs for the production schedule (internal/scheduling).
//
// These are the direct translations of the reads the knit-scheduling TS script did through Prisma. Everything the solver needs is loaded up front by these queries so the solver itself stays pure and testable against a fixture.
// GetConstraintMachines returns every machine in the constraint department.
//
// The department is the selection, not the machine: the knitting room sets the pace of the factory, so a machine added to it is planned without anyone remembering to tick a box. A machine that must sit out — down for a rebuild — is excluded by an explicit resource-setting row; the LEFT JOIN means the absence of a row means "planned", which is the safe default when the department is what was chosen.
func (q *Queries) GetConstraintMachines(ctx context.Context, arg GetConstraintMachinesParams) ([]GetConstraintMachinesRow, error) {
	rows, err := q.db.QueryContext(ctx, getConstraintMachines, arg.AccountID, arg.AccountID, arg.DepartmentID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []GetConstraintMachinesRow
	for rows.Next() {
		var i GetConstraintMachinesRow
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.DepartmentID,
			&i.ProductionStepID,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getCustomerFulfillmentProfiles = `-- name: GetCustomerFulfillmentProfiles :many
SELECT
    ar.counterparty_account_id AS customer_account_id,
    COALESCE(NULLIF(ar.alias, ''), a.name) AS customer_name,
    ar.default_lead_time_days AS customer_lead_time_days,
    ag.default_lead_time_days AS account_group_lead_time_days,
    ar.fulfillment_policy_code AS customer_policy,
    ag.fulfillment_policy_code AS account_group_policy
FROM account_relation ar
JOIN account a ON a.id = ar.counterparty_account_id
LEFT JOIN account_group ag ON ag.id = ar.account_group_id
WHERE ar.owner_account_id = ?
  AND ar.account_relation_role_code = 'customer'
ORDER BY ar.counterparty_account_id
`

type GetCustomerFulfillmentProfilesRow struct {
	CustomerAccountID        string
	CustomerName             string
	CustomerLeadTimeDays     sql.NullInt32
	AccountGroupLeadTimeDays sql.NullInt32
	CustomerPolicy           sql.NullString
	AccountGroupPolicy       sql.NullString
}

// GetCustomerFulfillmentProfiles returns every customer's lead time and stated policy, with the account group each falls back to.
//
// The recommendation asks two things of a customer: how long they allow, and how they say they buy. Both resolve through the same chain an order's ship-by is stamped from, so the two cannot disagree about the same customer.
func (q *Queries) GetCustomerFulfillmentProfiles(ctx context.Context, accountID string) ([]GetCustomerFulfillmentProfilesRow, error) {
	rows, err := q.db.QueryContext(ctx, getCustomerFulfillmentProfiles, accountID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []GetCustomerFulfillmentProfilesRow
	for rows.Next() {
		var i GetCustomerFulfillmentProfilesRow
		if err := rows.Scan(
			&i.CustomerAccountID,
			&i.CustomerName,
			&i.CustomerLeadTimeDays,
			&i.AccountGroupLeadTimeDays,
			&i.CustomerPolicy,
			&i.AccountGroupPolicy,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getEchelonOnHand = `-- name: GetEchelonOnHand :many
SELECT
    ir.item_id,
    CAST(COALESCE(SUM(
        -- Correlated per receipt: a grouped derived table cannot take the account and item filters and so aggregates all of inventory_allocation on every call.
        (q.value - COALESCE((
            SELECT SUM(aq.value)
            FROM inventory_allocation ia
            JOIN quantity aq ON aq.id = ia.quantity_id
            WHERE ia.inventory_receipt_id = ir.id
        ), 0)) * (u.ratio_numerator / u.ratio_denominator)
    ), 0) AS DECIMAL(65,30)) AS on_hand
FROM inventory_receipt ir
JOIN quantity q ON q.id = ir.quantity_id
JOIN unit u ON u.id = q.unit_id
WHERE ir.owner_account_id = ?
  AND ir.status_code = 'available'
  AND ir.item_id IN (/*SLICE:item_ids*/?)
GROUP BY ir.item_id
`

type GetEchelonOnHandParams struct {
	AccountID string
	ItemIds   []string
}

type GetEchelonOnHandRow struct {
	ItemID string
	OnHand string
}

// GetEchelonOnHand returns available inventory per item, net of what is already allocated. Quantities are normalized through the unit ratio so items stocked in different units are comparable.
func (q *Queries) GetEchelonOnHand(ctx context.Context, arg GetEchelonOnHandParams) ([]GetEchelonOnHandRow, error) {
	query := getEchelonOnHand
	var queryParams []interface{}
	queryParams = append(queryParams, arg.AccountID)
	if len(arg.ItemIds) > 0 {
		for _, v := range arg.ItemIds {
			queryParams = append(queryParams, v)
		}
		query = strings.Replace(query, "/*SLICE:item_ids*/?", strings.Repeat(",?", len(arg.ItemIds))[1:], 1)
	} else {
		query = strings.Replace(query, "/*SLICE:item_ids*/?", "NULL", 1)
	}
	rows, err := q.db.QueryContext(ctx, query, queryParams...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []GetEchelonOnHandRow
	for rows.Next() {
		var i GetEchelonOnHandRow
		if err := rows.Scan(&i.ItemID, &i.OnHand); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getItemUnitCosts = `-- name: GetItemUnitCosts :many
SELECT
    i.id AS item_id,
    r.value AS unit_cost
FROM item i
JOIN rate r ON r.id = i.unit_cost_id
WHERE i.account_id = ?
  AND i.id IN (/*SLICE:item_ids*/?)
`

type GetItemUnitCostsParams struct {
	AccountID string
	ItemIds   []string
}

type GetItemUnitCostsRow struct {
	ItemID   string
	UnitCost string
}

// GetItemUnitCosts returns each item's standard unit cost, which prices the holding side of a make-or-stock decision.
func (q *Queries) GetItemUnitCosts(ctx context.Context, arg GetItemUnitCostsParams) ([]GetItemUnitCostsRow, error) {
	query := getItemUnitCosts
	var queryParams []interface{}
	queryParams = append(queryParams, arg.AccountID)
	if len(arg.ItemIds) > 0 {
		for _, v := range arg.ItemIds {
			queryParams = append(queryParams, v)
		}
		query = strings.Replace(query, "/*SLICE:item_ids*/?", strings.Repeat(",?", len(arg.ItemIds))[1:], 1)
	} else {
		query = strings.Replace(query, "/*SLICE:item_ids*/?", "NULL", 1)
	}
	rows, err := q.db.QueryContext(ctx, query, queryParams...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []GetItemUnitCostsRow
	for rows.Next() {
		var i GetItemUnitCostsRow
		if err := rows.Scan(&i.ItemID, &i.UnitCost); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getItemsForProductLines = `-- name: GetItemsForProductLines :many
SELECT
    p.product_line_id,
    p.item_id
FROM product p
JOIN item i ON i.id = p.item_id
WHERE i.account_id = ?
  AND p.product_line_id IN (/*SLICE:product_line_ids*/?)
ORDER BY p.product_line_id, p.item_id
`

type GetItemsForProductLinesParams struct {
	AccountID      string
	ProductLineIds []sql.NullString
}

type GetItemsForProductLinesRow struct {
	ProductLineID sql.NullString
	ItemID        string
}

// GetItemsForProductLines resolves a product-line-scoped override onto its items so the distribution can be done per item.
func (q *Queries) GetItemsForProductLines(ctx context.Context, arg GetItemsForProductLinesParams) ([]GetItemsForProductLinesRow, error) {
	query := getItemsForProductLines
	var queryParams []interface{}
	queryParams = append(queryParams, arg.AccountID)
	if len(arg.ProductLineIds) > 0 {
		for _, v := range arg.ProductLineIds {
			queryParams = append(queryParams, v)
		}
		query = strings.Replace(query, "/*SLICE:product_line_ids*/?", strings.Repeat(",?", len(arg.ProductLineIds))[1:], 1)
	} else {
		query = strings.Replace(query, "/*SLICE:product_line_ids*/?", "NULL", 1)
	}
	rows, err := q.db.QueryContext(ctx, query, queryParams...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []GetItemsForProductLinesRow
	for rows.Next() {
		var i GetItemsForProductLinesRow
		if err := rows.Scan(&i.ProductLineID, &i.ItemID); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getOpenOrderRequirements = `-- name: GetOpenOrderRequirements :many
SELECT
    so.id AS sales_order_id,
    so.number AS sales_order_number,
    sol.id AS sales_order_line_id,
    so.ship_by_date,
    sol.product_id,
    CAST(
        q.value * (u.ratio_numerator / u.ratio_denominator)
        - COALESCE((
            SELECT SUM(plq.value * (plu.ratio_numerator / plu.ratio_denominator))
            FROM pick_line pl
            JOIN quantity plq ON plq.id = pl.quantity_id
            JOIN unit plu ON plu.id = plq.unit_id
            WHERE pl.sales_order_line_id = sol.id
              AND pl.packed_at IS NOT NULL
        ), 0)
    AS DECIMAL(65,30)) AS outstanding_quantity
FROM sales_order_line sol
JOIN sales_order so ON so.id = sol.sales_order_id
JOIN product p ON p.id = sol.product_id
JOIN quantity q ON q.id = sol.quantity_id
JOIN unit u ON u.id = q.unit_id
WHERE so.owner_account_id = ?
  AND so.sales_order_type_code = 'sales_order'
  AND so.sales_order_status_code = 'issued'
  AND p.product_type_code = 'sale'
  AND sol.product_id IN (/*SLICE:product_ids*/?)
ORDER BY so.ship_by_date, so.id, sol.id
`

type GetOpenOrderRequirementsParams struct {
	AccountID  string
	ProductIds []sql.NullString
}

type GetOpenOrderRequirementsRow struct {
	SalesOrderID        string
	SalesOrderNumber    string
	SalesOrderLineID    string
	ShipByDate          sql.NullTime
	ProductID           sql.NullString
	OutstandingQuantity string
}

// GetOpenOrderRequirements returns the outstanding quantity on every issued, unshipped order line, with the date it is due to ship.
//
// This is the order book the plan owes, as opposed to the history it forecasts from. Outstanding is ordered minus packed rather than ordered minus picked: a picked line is work in progress and still has to be produced if the stock is not there, while a packed one has left inventory. Estimates are excluded for the same reason GetPooledOrderDemandByProduct excludes them — an unissued quote is not demand — and non-sale lines (freight, credits) are excluded because they are not produced.
//
// ship_by_date is nullable: orders issued before commitments were tracked carry none. They are returned anyway and dated by the caller, because dropping real demand silently is worse than dating it badly and saying so.
func (q *Queries) GetOpenOrderRequirements(ctx context.Context, arg GetOpenOrderRequirementsParams) ([]GetOpenOrderRequirementsRow, error) {
	query := getOpenOrderRequirements
	var queryParams []interface{}
	queryParams = append(queryParams, arg.AccountID)
	if len(arg.ProductIds) > 0 {
		for _, v := range arg.ProductIds {
			queryParams = append(queryParams, v)
		}
		query = strings.Replace(query, "/*SLICE:product_ids*/?", strings.Repeat(",?", len(arg.ProductIds))[1:], 1)
	} else {
		query = strings.Replace(query, "/*SLICE:product_ids*/?", "NULL", 1)
	}
	rows, err := q.db.QueryContext(ctx, query, queryParams...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []GetOpenOrderRequirementsRow
	for rows.Next() {
		var i GetOpenOrderRequirementsRow
		if err := rows.Scan(
			&i.SalesOrderID,
			&i.SalesOrderNumber,
			&i.SalesOrderLineID,
			&i.ShipByDate,
			&i.ProductID,
			&i.OutstandingQuantity,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getPooledOrderDemandByProduct = `-- name: GetPooledOrderDemandByProduct :many
SELECT
    sol.product_id,
    YEAR(so.issued_at) AS demand_year,
    MONTH(so.issued_at) AS demand_month,
    CAST(COALESCE(SUM(q.value * (u.ratio_numerator / u.ratio_denominator)), 0) AS DECIMAL(65,30)) AS quantity
FROM sales_order_line sol
JOIN sales_order so ON so.id = sol.sales_order_id
JOIN quantity q ON q.id = sol.quantity_id
JOIN unit u ON u.id = q.unit_id
WHERE so.owner_account_id = ?
  AND so.sales_order_type_code = 'sales_order'
  AND so.sales_order_status_code <> 'estimate'
  AND so.issued_at IS NOT NULL
  AND so.issued_at >= ?
  AND so.issued_at <= ?
  AND sol.product_id IN (/*SLICE:product_ids*/?)
GROUP BY sol.product_id, YEAR(so.issued_at), MONTH(so.issued_at)
ORDER BY sol.product_id, demand_year, demand_month
`

type GetPooledOrderDemandByProductParams struct {
	AccountID   string
	WindowStart sql.NullTime
	WindowEnd   sql.NullTime
	ProductIds  []sql.NullString
}

type GetPooledOrderDemandByProductRow struct {
	ProductID   sql.NullString
	DemandYear  int32
	DemandMonth int32
	Quantity    string
}

// GetPooledOrderDemandByProduct returns monthly sold quantity per product, which is pooled back onto the constraint item that produces it.
//
// Estimates are excluded: an unissued quote is not demand.
func (q *Queries) GetPooledOrderDemandByProduct(ctx context.Context, arg GetPooledOrderDemandByProductParams) ([]GetPooledOrderDemandByProductRow, error) {
	query := getPooledOrderDemandByProduct
	var queryParams []interface{}
	queryParams = append(queryParams, arg.AccountID)
	queryParams = append(queryParams, arg.WindowStart)
	queryParams = append(queryParams, arg.WindowEnd)
	if len(arg.ProductIds) > 0 {
		for _, v := range arg.ProductIds {
			queryParams = append(queryParams, v)
		}
		query = strings.Replace(query, "/*SLICE:product_ids*/?", strings.Repeat(",?", len(arg.ProductIds))[1:], 1)
	} else {
		query = strings.Replace(query, "/*SLICE:product_ids*/?", "NULL", 1)
	}
	rows, err := q.db.QueryContext(ctx, query, queryParams...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []GetPooledOrderDemandByProductRow
	for rows.Next() {
		var i GetPooledOrderDemandByProductRow
		if err := rows.Scan(
			&i.ProductID,
			&i.DemandYear,
			&i.DemandMonth,
			&i.Quantity,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getProductDemandByCustomer = `-- name: GetProductDemandByCustomer :many
SELECT
    sol.product_id,
    so.buyer_account_id,
    YEAR(so.issued_at) AS demand_year,
    MONTH(so.issued_at) AS demand_month,
    CAST(COALESCE(SUM(q.value * (u.ratio_numerator / u.ratio_denominator)), 0) AS DECIMAL(65,30)) AS quantity
FROM sales_order_line sol
JOIN sales_order so ON so.id = sol.sales_order_id
JOIN quantity q ON q.id = sol.quantity_id
JOIN unit u ON u.id = q.unit_id
WHERE so.owner_account_id = ?
  AND so.sales_order_type_code = 'sales_order'
  AND so.sales_order_status_code <> 'estimate'
  AND so.issued_at IS NOT NULL
  AND so.issued_at >= ?
  AND so.issued_at <= ?
  AND sol.product_id IN (/*SLICE:product_ids*/?)
GROUP BY sol.product_id, so.buyer_account_id, YEAR(so.issued_at), MONTH(so.issued_at)
ORDER BY sol.product_id, so.buyer_account_id, demand_year, demand_month
`

type GetProductDemandByCustomerParams struct {
	AccountID   string
	WindowStart sql.NullTime
	WindowEnd   sql.NullTime
	ProductIds  []sql.NullString
}

type GetProductDemandByCustomerRow struct {
	ProductID      sql.NullString
	BuyerAccountID string
	DemandYear     int32
	DemandMonth    int32
	Quantity       string
}

// GetProductDemandByCustomer returns monthly sold quantity per product AND buyer, which is what the fulfillment recommendation measures customer concentration from.
//
// Deliberately separate from GetPooledOrderDemandByProduct rather than a widening of it: the solve pools demand and never needs to know who bought, and carrying a customer dimension through every plan to serve a recommendation nobody asked for would make every solve heavier.
func (q *Queries) GetProductDemandByCustomer(ctx context.Context, arg GetProductDemandByCustomerParams) ([]GetProductDemandByCustomerRow, error) {
	query := getProductDemandByCustomer
	var queryParams []interface{}
	queryParams = append(queryParams, arg.AccountID)
	queryParams = append(queryParams, arg.WindowStart)
	queryParams = append(queryParams, arg.WindowEnd)
	if len(arg.ProductIds) > 0 {
		for _, v := range arg.ProductIds {
			queryParams = append(queryParams, v)
		}
		query = strings.Replace(query, "/*SLICE:product_ids*/?", strings.Repeat(",?", len(arg.ProductIds))[1:], 1)
	} else {
		query = strings.Replace(query, "/*SLICE:product_ids*/?", "NULL", 1)
	}
	rows, err := q.db.QueryContext(ctx, query, queryParams...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []GetProductDemandByCustomerRow
	for rows.Next() {
		var i GetProductDemandByCustomerRow
		if err := rows.Scan(
			&i.ProductID,
			&i.BuyerAccountID,
			&i.DemandYear,
			&i.DemandMonth,
			&i.Quantity,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getProductionScheduleItemSettings = `-- name: GetProductionScheduleItemSettings :many
SELECT
    s.item_id,
    s.is_excluded,
    s.lot_multiple_units,
    s.fulfillment_policy_code
FROM production_schedule_item_setting s
WHERE s.account_id = ?
`

type GetProductionScheduleItemSettingsRow struct {
	ItemID                string
	IsExcluded            bool
	LotMultipleUnits      sql.NullString
	FulfillmentPolicyCode sql.NullString
}

// GetProductionScheduleItemSettings returns per-item planning overrides.
func (q *Queries) GetProductionScheduleItemSettings(ctx context.Context, accountID string) ([]GetProductionScheduleItemSettingsRow, error) {
	rows, err := q.db.QueryContext(ctx, getProductionScheduleItemSettings, accountID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []GetProductionScheduleItemSettingsRow
	for rows.Next() {
		var i GetProductionScheduleItemSettingsRow
		if err := rows.Scan(
			&i.ItemID,
			&i.IsExcluded,
			&i.LotMultipleUnits,
			&i.FulfillmentPolicyCode,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getProductsForItems = `-- name: GetProductsForItems :many
SELECT
    p.id AS product_id,
    p.item_id,
    i.sku,
    p.product_line_id
FROM product p
JOIN item i ON i.id = p.item_id
WHERE i.account_id = ?
  AND p.item_id IN (/*SLICE:item_ids*/?)
ORDER BY p.item_id
`

type GetProductsForItemsParams struct {
	AccountID string
	ItemIds   []string
}

type GetProductsForItemsRow struct {
	ProductID     string
	ItemID        string
	Sku           string
	ProductLineID sql.NullString
}

// GetProductsForItems returns the sellable products for a set of items, with the SKU and product line each one carries so a finished good can be reported by name rather than by ID. Only items with a product carry order demand.
func (q *Queries) GetProductsForItems(ctx context.Context, arg GetProductsForItemsParams) ([]GetProductsForItemsRow, error) {
	query := getProductsForItems
	var queryParams []interface{}
	queryParams = append(queryParams, arg.AccountID)
	if len(arg.ItemIds) > 0 {
		for _, v := range arg.ItemIds {
			queryParams = append(queryParams, v)
		}
		query = strings.Replace(query, "/*SLICE:item_ids*/?", strings.Repeat(",?", len(arg.ItemIds))[1:], 1)
	} else {
		query = strings.Replace(query, "/*SLICE:item_ids*/?", "NULL", 1)
	}
	rows, err := q.db.QueryContext(ctx, query, queryParams...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []GetProductsForItemsRow
	for rows.Next() {
		var i GetProductsForItemsRow
		if err := rows.Scan(
			&i.ProductID,
			&i.ItemID,
			&i.Sku,
			&i.ProductLineID,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getSeedBatchesForItems = `-- name: GetSeedBatchesForItems :many
SELECT
    b.id AS batch_id,
    b.item_id
FROM batch b
WHERE b.account_id = ?
  AND b.item_id IN (/*SLICE:item_ids*/?)
  AND b.scanned_at IS NOT NULL
  AND b.scanned_at >= ?
  AND b.scanned_at <= ?
ORDER BY b.item_id, b.scanned_at DESC, b.id DESC
`

type GetSeedBatchesForItemsParams struct {
	AccountID   string
	ItemIds     []string
	WindowStart sql.NullTime
	WindowEnd   sql.NullTime
}

type GetSeedBatchesForItemsRow struct {
	BatchID string
	ItemID  string
}

// GetSeedBatchesForItems returns the batches to start the genealogy walk from: every scan of the item inside the demand window. Seeding from the whole window rather than a recent sample is what keeps the echelon complete — stock held as a finished good only an older batch flowed to still has to count against the decision to build more.
func (q *Queries) GetSeedBatchesForItems(ctx context.Context, arg GetSeedBatchesForItemsParams) ([]GetSeedBatchesForItemsRow, error) {
	query := getSeedBatchesForItems
	var queryParams []interface{}
	queryParams = append(queryParams, arg.AccountID)
	if len(arg.ItemIds) > 0 {
		for _, v := range arg.ItemIds {
			queryParams = append(queryParams, v)
		}
		query = strings.Replace(query, "/*SLICE:item_ids*/?", strings.Repeat(",?", len(arg.ItemIds))[1:], 1)
	} else {
		query = strings.Replace(query, "/*SLICE:item_ids*/?", "NULL", 1)
	}
	queryParams = append(queryParams, arg.WindowStart)
	queryParams = append(queryParams, arg.WindowEnd)
	rows, err := q.db.QueryContext(ctx, query, queryParams...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []GetSeedBatchesForItemsRow
	for rows.Next() {
		var i GetSeedBatchesForItemsRow
		if err := rows.Scan(&i.BatchID, &i.ItemID); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getStepConsumptionItems = `-- name: GetStepConsumptionItems :many
SELECT
    c.production_step_id,
    c.item_id,
    i.sku
FROM consumption c
JOIN item i ON i.id = c.item_id
WHERE c.production_step_id IN (/*SLICE:production_step_ids*/?)
ORDER BY c.production_step_id, i.sku
`

type GetStepConsumptionItemsRow struct {
	ProductionStepID sql.NullString
	ItemID           string
	Sku              string
}

// GetStepConsumptionItems returns the input items each production step consumes. The count of inputs a product introduces relative to the previous one is what drives the changeover model.
func (q *Queries) GetStepConsumptionItems(ctx context.Context, productionStepIds []sql.NullString) ([]GetStepConsumptionItemsRow, error) {
	query := getStepConsumptionItems
	var queryParams []interface{}
	if len(productionStepIds) > 0 {
		for _, v := range productionStepIds {
			queryParams = append(queryParams, v)
		}
		query = strings.Replace(query, "/*SLICE:production_step_ids*/?", strings.Repeat(",?", len(productionStepIds))[1:], 1)
	} else {
		query = strings.Replace(query, "/*SLICE:production_step_ids*/?", "NULL", 1)
	}
	rows, err := q.db.QueryContext(ctx, query, queryParams...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []GetStepConsumptionItemsRow
	for rows.Next() {
		var i GetStepConsumptionItemsRow
		if err := rows.Scan(&i.ProductionStepID, &i.ItemID, &i.Sku); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
