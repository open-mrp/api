// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: territory.sql

package sqlc

import (
	"context"
	"database/sql"
	"time"
)

const checkTerritoryInAccount = `-- name: CheckTerritoryInAccount :one
SELECT EXISTS(
    SELECT 1 FROM territory
    WHERE id = ?
    AND account_id = ?
) AS ` + "`" + `exists` + "`" + `
`

type CheckTerritoryInAccountParams struct {
	ID        string
	AccountID string
}

func (q *Queries) CheckTerritoryInAccount(ctx context.Context, arg CheckTerritoryInAccountParams) (bool, error) {
	row := q.db.QueryRowContext(ctx, checkTerritoryInAccount, arg.ID, arg.AccountID)
	var exists bool
	err := row.Scan(&exists)
	return exists, err
}

const deleteTerritory = `-- name: DeleteTerritory :exec
DELETE FROM territory
WHERE id = ?
AND account_id = ?
`

type DeleteTerritoryParams struct {
	ID        string
	AccountID string
}

func (q *Queries) DeleteTerritory(ctx context.Context, arg DeleteTerritoryParams) error {
	_, err := q.db.ExecContext(ctx, deleteTerritory, arg.ID, arg.AccountID)
	return err
}

const findSalesRepByState = `-- name: FindSalesRepByState :one
SELECT t.sales_rep_id
FROM territory t
WHERE t.account_id = ?
AND t.state = ?
AND t.start_zipcode IS NULL
LIMIT 1
`

type FindSalesRepByStateParams struct {
	AccountID string
	State     string
}

func (q *Queries) FindSalesRepByState(ctx context.Context, arg FindSalesRepByStateParams) (string, error) {
	row := q.db.QueryRowContext(ctx, findSalesRepByState, arg.AccountID, arg.State)
	var sales_rep_id string
	err := row.Scan(&sales_rep_id)
	return sales_rep_id, err
}

const findSalesRepByZipcode = `-- name: FindSalesRepByZipcode :one
SELECT t.sales_rep_id
FROM territory t
WHERE t.account_id = ?
AND (
    (t.start_zipcode <= ? AND t.end_zipcode >= ?)
    OR (t.start_zipcode = ? AND t.end_zipcode IS NULL)
)
LIMIT 1
`

type FindSalesRepByZipcodeParams struct {
	AccountID string
	Zipcode   sql.NullInt32
}

func (q *Queries) FindSalesRepByZipcode(ctx context.Context, arg FindSalesRepByZipcodeParams) (string, error) {
	row := q.db.QueryRowContext(ctx, findSalesRepByZipcode,
		arg.AccountID,
		arg.Zipcode,
		arg.Zipcode,
		arg.Zipcode,
	)
	var sales_rep_id string
	err := row.Scan(&sales_rep_id)
	return sales_rep_id, err
}

const getTerritory = `-- name: GetTerritory :one
SELECT
    t.id,
    t.state,
    t.start_zipcode,
    t.end_zipcode,
    t.sales_rep_id,
    t.product_line_id,
    t.created_at,
    t.updated_at,
    u.name AS sales_rep_name,
    u.email AS sales_rep_email,
    au.status_code AS sales_rep_status,
    au.created_at AS sales_rep_created_at,
    au.updated_at AS sales_rep_updated_at,
    pl.name AS product_line_name,
    pl.is_commission_exempt AS product_line_is_commission_exempt,
    pl.is_freight_exempt AS product_line_is_freight_exempt,
    pl.created_at AS product_line_created_at,
    pl.updated_at AS product_line_updated_at
FROM territory t
JOIN account_user au ON au.id = t.sales_rep_id
JOIN user u ON u.id = au.user_id
LEFT JOIN product_line pl ON pl.id = t.product_line_id
WHERE t.id = ?
AND t.account_id = ?
`

type GetTerritoryParams struct {
	ID        string
	AccountID string
}

type GetTerritoryRow struct {
	ID                            string
	State                         string
	StartZipcode                  sql.NullInt32
	EndZipcode                    sql.NullInt32
	SalesRepID                    string
	ProductLineID                 sql.NullString
	CreatedAt                     time.Time
	UpdatedAt                     time.Time
	SalesRepName                  sql.NullString
	SalesRepEmail                 sql.NullString
	SalesRepStatus                string
	SalesRepCreatedAt             time.Time
	SalesRepUpdatedAt             time.Time
	ProductLineName               sql.NullString
	ProductLineIsCommissionExempt sql.NullBool
	ProductLineIsFreightExempt    sql.NullBool
	ProductLineCreatedAt          sql.NullTime
	ProductLineUpdatedAt          sql.NullTime
}

func (q *Queries) GetTerritory(ctx context.Context, arg GetTerritoryParams) (GetTerritoryRow, error) {
	row := q.db.QueryRowContext(ctx, getTerritory, arg.ID, arg.AccountID)
	var i GetTerritoryRow
	err := row.Scan(
		&i.ID,
		&i.State,
		&i.StartZipcode,
		&i.EndZipcode,
		&i.SalesRepID,
		&i.ProductLineID,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.SalesRepName,
		&i.SalesRepEmail,
		&i.SalesRepStatus,
		&i.SalesRepCreatedAt,
		&i.SalesRepUpdatedAt,
		&i.ProductLineName,
		&i.ProductLineIsCommissionExempt,
		&i.ProductLineIsFreightExempt,
		&i.ProductLineCreatedAt,
		&i.ProductLineUpdatedAt,
	)
	return i, err
}

const insertTerritory = `-- name: InsertTerritory :exec
INSERT INTO territory (
    id,
    state,
    start_zipcode,
    end_zipcode,
    sales_rep_id,
    account_id,
    product_line_id,
    created_at,
    updated_at
) VALUES (
    ?,
    ?,
    ?,
    ?,
    ?,
    ?,
    ?,
    NOW(3),
    NOW(3)
)
`

type InsertTerritoryParams struct {
	ID            string
	State         string
	StartZipcode  sql.NullInt32
	EndZipcode    sql.NullInt32
	SalesRepID    string
	AccountID     string
	ProductLineID sql.NullString
}

func (q *Queries) InsertTerritory(ctx context.Context, arg InsertTerritoryParams) error {
	_, err := q.db.ExecContext(ctx, insertTerritory,
		arg.ID,
		arg.State,
		arg.StartZipcode,
		arg.EndZipcode,
		arg.SalesRepID,
		arg.AccountID,
		arg.ProductLineID,
	)
	return err
}

const listTerritoriesBackward = `-- name: ListTerritoriesBackward :many
SELECT
    t.id,
    t.state,
    t.start_zipcode,
    t.end_zipcode,
    t.sales_rep_id,
    t.product_line_id,
    t.created_at,
    t.updated_at,
    u.name AS sales_rep_name,
    u.email AS sales_rep_email,
    au.status_code AS sales_rep_status,
    au.created_at AS sales_rep_created_at,
    au.updated_at AS sales_rep_updated_at,
    pl.name AS product_line_name,
    pl.is_commission_exempt AS product_line_is_commission_exempt,
    pl.is_freight_exempt AS product_line_is_freight_exempt,
    pl.created_at AS product_line_created_at,
    pl.updated_at AS product_line_updated_at
FROM territory t
JOIN account_user au ON au.id = t.sales_rep_id
JOIN user u ON u.id = au.user_id
LEFT JOIN product_line pl ON pl.id = t.product_line_id
WHERE t.account_id = ?
AND (
    ? IS NULL
    OR t.state LIKE ?
    OR u.name LIKE ?
    OR u.email LIKE ?
    OR pl.name LIKE ?
)
AND (
    ? IS NULL
    OR (
        (t.start_zipcode IS NULL OR t.start_zipcode <= CAST(? AS SIGNED))
        AND (
            (t.end_zipcode IS NULL AND t.start_zipcode = CAST(? AS SIGNED))
            OR t.end_zipcode >= CAST(? AS SIGNED)
        )
    )
)
AND (
    t.created_at > ?
    OR (t.created_at = ? AND t.id > ?)
)
ORDER BY t.created_at ASC, t.id ASC
LIMIT ?
`

type ListTerritoriesBackwardParams struct {
	AccountID       string
	SearchQuery     sql.NullString
	ZipcodeQuery    sql.NullInt64
	CursorCreatedAt time.Time
	CursorID        string
	Limit           int32
}

type ListTerritoriesBackwardRow struct {
	ID                            string
	State                         string
	StartZipcode                  sql.NullInt32
	EndZipcode                    sql.NullInt32
	SalesRepID                    string
	ProductLineID                 sql.NullString
	CreatedAt                     time.Time
	UpdatedAt                     time.Time
	SalesRepName                  sql.NullString
	SalesRepEmail                 sql.NullString
	SalesRepStatus                string
	SalesRepCreatedAt             time.Time
	SalesRepUpdatedAt             time.Time
	ProductLineName               sql.NullString
	ProductLineIsCommissionExempt sql.NullBool
	ProductLineIsFreightExempt    sql.NullBool
	ProductLineCreatedAt          sql.NullTime
	ProductLineUpdatedAt          sql.NullTime
}

func (q *Queries) ListTerritoriesBackward(ctx context.Context, arg ListTerritoriesBackwardParams) ([]ListTerritoriesBackwardRow, error) {
	rows, err := q.db.QueryContext(ctx, listTerritoriesBackward,
		arg.AccountID,
		arg.SearchQuery,
		arg.SearchQuery,
		arg.SearchQuery,
		arg.SearchQuery,
		arg.SearchQuery,
		arg.ZipcodeQuery,
		arg.ZipcodeQuery,
		arg.ZipcodeQuery,
		arg.ZipcodeQuery,
		arg.CursorCreatedAt,
		arg.CursorCreatedAt,
		arg.CursorID,
		arg.Limit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListTerritoriesBackwardRow
	for rows.Next() {
		var i ListTerritoriesBackwardRow
		if err := rows.Scan(
			&i.ID,
			&i.State,
			&i.StartZipcode,
			&i.EndZipcode,
			&i.SalesRepID,
			&i.ProductLineID,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.SalesRepName,
			&i.SalesRepEmail,
			&i.SalesRepStatus,
			&i.SalesRepCreatedAt,
			&i.SalesRepUpdatedAt,
			&i.ProductLineName,
			&i.ProductLineIsCommissionExempt,
			&i.ProductLineIsFreightExempt,
			&i.ProductLineCreatedAt,
			&i.ProductLineUpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listTerritoriesForward = `-- name: ListTerritoriesForward :many
SELECT
    t.id,
    t.state,
    t.start_zipcode,
    t.end_zipcode,
    t.sales_rep_id,
    t.product_line_id,
    t.created_at,
    t.updated_at,
    u.name AS sales_rep_name,
    u.email AS sales_rep_email,
    au.status_code AS sales_rep_status,
    au.created_at AS sales_rep_created_at,
    au.updated_at AS sales_rep_updated_at,
    pl.name AS product_line_name,
    pl.is_commission_exempt AS product_line_is_commission_exempt,
    pl.is_freight_exempt AS product_line_is_freight_exempt,
    pl.created_at AS product_line_created_at,
    pl.updated_at AS product_line_updated_at
FROM territory t
JOIN account_user au ON au.id = t.sales_rep_id
JOIN user u ON u.id = au.user_id
LEFT JOIN product_line pl ON pl.id = t.product_line_id
WHERE t.account_id = ?
AND (
    ? IS NULL
    OR t.state LIKE ?
    OR u.name LIKE ?
    OR u.email LIKE ?
    OR pl.name LIKE ?
)
AND (
    ? IS NULL
    OR (
        (t.start_zipcode IS NULL OR t.start_zipcode <= CAST(? AS SIGNED))
        AND (
            (t.end_zipcode IS NULL AND t.start_zipcode = CAST(? AS SIGNED))
            OR t.end_zipcode >= CAST(? AS SIGNED)
        )
    )
)
AND (
    ? IS NULL
    OR t.created_at < ?
    OR (t.created_at = ? AND t.id < ?)
)
ORDER BY t.created_at DESC, t.id DESC
LIMIT ?
`

type ListTerritoriesForwardParams struct {
	AccountID       string
	SearchQuery     sql.NullString
	ZipcodeQuery    sql.NullInt64
	CursorCreatedAt sql.NullTime
	CursorID        sql.NullString
	Limit           int32
}

type ListTerritoriesForwardRow struct {
	ID                            string
	State                         string
	StartZipcode                  sql.NullInt32
	EndZipcode                    sql.NullInt32
	SalesRepID                    string
	ProductLineID                 sql.NullString
	CreatedAt                     time.Time
	UpdatedAt                     time.Time
	SalesRepName                  sql.NullString
	SalesRepEmail                 sql.NullString
	SalesRepStatus                string
	SalesRepCreatedAt             time.Time
	SalesRepUpdatedAt             time.Time
	ProductLineName               sql.NullString
	ProductLineIsCommissionExempt sql.NullBool
	ProductLineIsFreightExempt    sql.NullBool
	ProductLineCreatedAt          sql.NullTime
	ProductLineUpdatedAt          sql.NullTime
}

func (q *Queries) ListTerritoriesForward(ctx context.Context, arg ListTerritoriesForwardParams) ([]ListTerritoriesForwardRow, error) {
	rows, err := q.db.QueryContext(ctx, listTerritoriesForward,
		arg.AccountID,
		arg.SearchQuery,
		arg.SearchQuery,
		arg.SearchQuery,
		arg.SearchQuery,
		arg.SearchQuery,
		arg.ZipcodeQuery,
		arg.ZipcodeQuery,
		arg.ZipcodeQuery,
		arg.ZipcodeQuery,
		arg.CursorCreatedAt,
		arg.CursorCreatedAt,
		arg.CursorCreatedAt,
		arg.CursorID,
		arg.Limit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListTerritoriesForwardRow
	for rows.Next() {
		var i ListTerritoriesForwardRow
		if err := rows.Scan(
			&i.ID,
			&i.State,
			&i.StartZipcode,
			&i.EndZipcode,
			&i.SalesRepID,
			&i.ProductLineID,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.SalesRepName,
			&i.SalesRepEmail,
			&i.SalesRepStatus,
			&i.SalesRepCreatedAt,
			&i.SalesRepUpdatedAt,
			&i.ProductLineName,
			&i.ProductLineIsCommissionExempt,
			&i.ProductLineIsFreightExempt,
			&i.ProductLineCreatedAt,
			&i.ProductLineUpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateTerritory = `-- name: UpdateTerritory :exec
UPDATE territory SET
    state = COALESCE(?, state),
    start_zipcode = CASE
        WHEN ? = TRUE THEN NULL
        WHEN ? IS NOT NULL THEN ?
        ELSE start_zipcode
    END,
    end_zipcode = CASE
        WHEN ? = TRUE THEN NULL
        WHEN ? IS NOT NULL THEN ?
        ELSE end_zipcode
    END,
    sales_rep_id = ?,
    product_line_id = CASE
        WHEN ? = TRUE THEN NULL
        WHEN ? IS NOT NULL THEN ?
        ELSE product_line_id
    END,
    updated_at = NOW(3)
WHERE id = ?
AND account_id = ?
`

type UpdateTerritoryParams struct {
	State             sql.NullString
	ClearStartZipcode interface{}
	StartZipcode      sql.NullInt32
	ClearEndZipcode   interface{}
	EndZipcode        sql.NullInt32
	SalesRepID        sql.NullString
	ClearProductLine  interface{}
	ProductLineID     sql.NullString
	ID                string
	AccountID         string
}

func (q *Queries) UpdateTerritory(ctx context.Context, arg UpdateTerritoryParams) error {
	_, err := q.db.ExecContext(ctx, updateTerritory,
		arg.State,
		arg.ClearStartZipcode,
		arg.StartZipcode,
		arg.StartZipcode,
		arg.ClearEndZipcode,
		arg.EndZipcode,
		arg.EndZipcode,
		arg.SalesRepID,
		arg.ClearProductLine,
		arg.ProductLineID,
		arg.ProductLineID,
		arg.ID,
		arg.AccountID,
	)
	return err
}
