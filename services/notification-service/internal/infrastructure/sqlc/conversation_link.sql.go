// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: conversation_link.sql

package sqlc

import (
	"context"
	"database/sql"
)

const createConversationLink = `-- name: CreateConversationLink :exec
INSERT INTO conversation_link (
    id, account_id, conversation_id, resource_type, resource_id, created_by_participant_id, created_at
) VALUES (?, ?, ?, ?, ?, ?, NOW(3))
`

type CreateConversationLinkParams struct {
	ID                     string
	AccountID              string
	ConversationID         string
	ResourceType           string
	ResourceID             string
	CreatedByParticipantID sql.NullString
}

// Adds a secondary business-record link to a conversation. Idempotent on the unique
// (conversation_id, resource_type, resource_id) — a duplicate insert is mapped to a conflict.
func (q *Queries) CreateConversationLink(ctx context.Context, arg CreateConversationLinkParams) error {
	_, err := q.db.ExecContext(ctx, createConversationLink,
		arg.ID,
		arg.AccountID,
		arg.ConversationID,
		arg.ResourceType,
		arg.ResourceID,
		arg.CreatedByParticipantID,
	)
	return err
}

const deleteConversationLink = `-- name: DeleteConversationLink :execrows
DELETE FROM conversation_link
WHERE id = ? AND conversation_id = ? AND account_id = ?
`

type DeleteConversationLinkParams struct {
	ID             string
	ConversationID string
	AccountID      string
}

func (q *Queries) DeleteConversationLink(ctx context.Context, arg DeleteConversationLinkParams) (int64, error) {
	result, err := q.db.ExecContext(ctx, deleteConversationLink, arg.ID, arg.ConversationID, arg.AccountID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected()
}

const listConversationLinks = `-- name: ListConversationLinks :many
SELECT id, account_id, conversation_id, resource_type, resource_id, created_by_participant_id, created_at FROM conversation_link
WHERE conversation_id = ? AND account_id = ?
ORDER BY created_at ASC, id ASC
`

type ListConversationLinksParams struct {
	ConversationID string
	AccountID      string
}

func (q *Queries) ListConversationLinks(ctx context.Context, arg ListConversationLinksParams) ([]ConversationLink, error) {
	rows, err := q.db.QueryContext(ctx, listConversationLinks, arg.ConversationID, arg.AccountID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ConversationLink
	for rows.Next() {
		var i ConversationLink
		if err := rows.Scan(
			&i.ID,
			&i.AccountID,
			&i.ConversationID,
			&i.ResourceType,
			&i.ResourceID,
			&i.CreatedByParticipantID,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
